fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic-settings==2.5.2
eval_type_backport>=0.2.0
python-multipart==0.0.12
pypdf==4.3.1
scikit-learn==1.5.2
numpy==1.26.4
requests==2.32.3
psutil>=5.9  # used by the upload endpoint's memory instrumentation
firebase-admin==6.5.0
google-genai>=0.1.0

# Azure services (all required now that real credentials are configured)
azure-ai-documentintelligence==1.0.0
azure-search-documents==11.5.1
azure-storage-blob==12.22.0
azure-cosmos==4.7.0
azure-identity==1.19.0

# Azure AI Foundry Agent Service (v2 SDK -- current API as of 2026,
# the old threads/runs API in azure-ai-agents is retired). This pulls in
# a compatible `openai` client version itself (must support the
# Responses/Conversations API) -- don't pin openai separately above it.
azure-ai-projects>=2.3.0
openai>=1.66.0
